source code:
    the code that programmer writes and understands

machine code:
    the code that computers understands(0s and 1s)

SO WE HAVE TO CONVERT OUR SOURCE CODE TO MACHINE CODE so THE MACHINE COULD UNDERSTAND IT 

        source code ----> compiler ------> machine code     
            # compiler is a piece of software that other peopple have written that we can use to convert a specific syntax to byte code/machine code

when running a programe we have to 1st compile it {make filenameWithoutSuffix}
and then run it {./hello  -> means somewhere in my current directory there is hello file which is alreday compiled (will create a new file)}  
    if change source code and not compile the new version the machine code gonna stay the saame ad givrs the same output 


header files:
    files that ends om .h 
    stdio.h
    contains code that other people wrote that we can use in our own programes 
    so the header files gives us access to library 
        so if wnna use printfwe have to include the header file that definese that func 
            i this case the header file is stdio.h {standard io . h}

docs for c:
    the official manuall pages(man pages)


    arguments ---> function ---> sideeffect / reurn value      # A return value is the direct output a function sends back, while       side effect is any change a function makes outside its local scope